# Core dependencies (REQUIRED for all features)
google-cloud-speech==2.26.0
google-cloud-texttospeech==2.16.3
google-auth==2.29.0
python-dotenv==1.0.1
requests==2.31.0
pyyaml==6.0.1
numpy==1.26.4

# Audio processing (OPTIONAL - only needed for voice input/output)
# You can still run the demo and text-only mode without these
pyaudio==0.2.14
pydub==0.25.1
soundfile==0.12.1

# NLP and ML (OPTIONAL - for advanced features)
# Uncomment these if you want to use ML-based intent recognition
# transformers==4.40.0
# torch==2.2.2
# nltk==3.8.1
# spacy==3.7.4

# Web framework (for web interface)
flask==3.0.3
flask-cors==4.0.0

# Streamlit (for interactive web UI)
streamlit==1.31.1
audio-recorder-streamlit==0.0.8

# OpenAI (for AI-powered responses - OPTIONAL)
openai==1.12.0
